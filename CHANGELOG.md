# 更新紀錄

## Unreleased — Markdown 雙模式與文件匯入強化

- 卡片編輯器新增「富文字／Markdown」雙模式：兩種模式是同一份 TipTap 文件的投影，只保存 `contentHtml`，往復切換維持標題、清單、表格、程式碼與核取清單的語意；核取清單以文字、階層與位置比對既有 `data-task-id`，已同步待辦不會被重建。Markdown 模式為等寬文字框，支援 Tab 縮排、卡內搜尋、IME 組字、420ms 自動儲存與關閉前 flush，並在本機記住上次的模式選擇。
- 新增集中式內容管線（`contentPipeline`）：`fromMarkdown`／`fromHtml`／`fromPlainText`／`toMarkdown`／`appendContent` 統一產出 `contentHtml` 與 `plainText`，RichEditor、日誌、AI 動作、MCP、白板與匯入器不再各自拼接 HTML。TipTap Markdown 引擎以固定版本包住，只有 `markdownBridge` 允許直接 import。
- 文件匯入改為一檔一卡的獨立交易：MD、TXT、HTML、DOCX、PDF 各自解析、失敗只回收該檔的來源與內嵌附件，批次可部分成功並保留逐檔結果與開卡入口；超過大小上限（文字 20 MB、DOCX 50 MB、PDF 100 MB）的檔案不建立半成品卡片。
- Markdown／HTML 中的相对圖片與 data URL 圖片轉為 `role: "inline"` 附件，正文以 `attachment://<id>` 表示；網路圖片先列出來源網域徵求同意，預設不連線，未下載者保留 alt 與網址的文字占位，不留會發請求的追蹤圖片。找不到來源資料夾的相對圖片顯示占位而不是破圖。
- 桌面與 Android 新增 `documents.resolveLocalAssets`／`documents.downloadRemoteAssets` 橋接：桌面只讀取來源文件所在資料夾並擋掉路徑穿越；Android 以 Storage Access Framework 讓使用者一次授權資源資料夾，持久化讀取權限，重啟後仍可用；遠端下載逐次檢查重新導向、私有位址、MIME 魔法檔頭與大小上限。
- 新增單一卡片「匯出 Markdown」：無圖片輸出 `.md`，有圖片輸出含 `assets/` 的 ZIP；全庫 Markdown 備份改用真正的 TipTap Markdown serializer，不再以 `plainText` 代替格式。附件新增可選 `role` 欄位，JSON 備份與同步協定保持相容，無需破壞性遷移。
- 匯入與雙模式的進度、同意、警告與失敗文案提供繁中、簡中、英、日、韓五語，並新增文案完整性測試。
- 測試：新增固定 DOCX／HTML／PDF fixture 的匯入測試、安全測試（script、`onerror`、`javascript:`、私有 IP、路徑穿越）、核取清單 ID 往返測試與五語文案測試。
- 自訂 AI Provider 的 HTTP 連線放寬至 localhost、loopback、RFC1918 私有網段與 IPv6 ULA；Android 同步允許私有區域網路 HTTP，仍拒絕公開網路的 HTTP Provider。

## 0.9.5 — 2026-09-06

- Google 雲端在資料修改後停筆 30 秒備份；閱讀、滑動和點擊不再阻止備份。持續編輯時仍按設定間隔備份。
- 未完成的資料變更保留至下次啟動補傳，上傳期間的新修改不會被舊備份誤標為完成。
- 完整退出前等待最後雲端備份；失敗或逾時提供重試、返回澄境與仍然退出。跨裝置衝突仍需由使用者處理。
- 本地與雲端分別處理完成與失敗，保留原本本地備份週期與前一天救援機制。

## 0.9.4 — 2026-09-06

- 修正 Gemini 3.8 Flash 在一般聊天正常、第二大腦「AI 整理連結」卻因上游拒絕完整 JSON Schema 而失敗；現在會依序嘗試 Schema、JSON 物件與純 JSON 指令，最後仍由澄境本機驗證內容。
- 第二大腦對推理型模型使用低推理強度並隱藏推理內容，提供較充足的正式答案空間；只有輸出確實不完整時才提高上限重試，避免無限制重送。
- 解析器新增不同模型常見的頂層陣列、`links`／`relations`／`edges`、`from`／`to`、關係名稱、百分比信心值與證據物件相容處理。
- 模型拒絕 `reasoning` 或 `temperature` 時只移除不相容的選填參數；空白回覆會切換可用 Provider 路徑重試一次，外部服務、額度與權限錯誤仍保留真正原因。
- 以 OpenRouter 真實請求驗證 Gemini 3.8 Flash 與 DeepSeek V4 Flash 0731，兩者皆能從相同 40 筆合成神經元建立 12 條可解析連結。
- 左側功能在游標準備點擊或鍵盤聚焦時先載入程式模組；尚未完成前保留目前工作區，不再閃出整頁讀取骨架，也不會在啟動時一次載入所有功能。
- 第二大腦重用斷詞器與日期格式器，關鍵字直接累計而不建立重複的大型中間陣列；完整資料與舊神經元搜尋範圍維持不變。

## 0.9.3 — 2026-09-05

- 修正全域復原記錄未完成或已取消的交易，以及背景維護可能吞掉同時發生的使用者操作；復原與重做只處理成功提交的變更。
- 修正同名筆記在 Markdown 匯出時互相覆蓋；檔名也相容 Windows 的大小寫與保留名稱規則。
- 復原備份前驗證資料表與重複 ID；本地復原也會先建立安全副本。附件先驗證內容再使用獨立路徑保存，避免失敗的復原覆寫原檔。
- 移除的附件保留到本次操作歷史結束，讓 Undo 能找回檔案；下次啟動只清理仍未被引用的待移除附件。
- MCP 追加筆記保留原有格式；版本檢查與修改在同一交易內進行。新增子待辦會同步母項目與原卡片，背景同步保留既有父子關係。
- 搜尋先確認符合條件才限制結果筆數，並補上單字元、全半形與長文尾端的查找；資料庫、卡片庫和 MCP 不再建立整批候選陣列。
- 第二大腦採按需繪製：靜止且沒有動畫的測試畫面從每秒約 15,120 次繪圖呼叫降到 0，鏡頭操作、選取與共享神經元動畫維持正常。
- 修正待辦數量不顯示、垃圾桶關聯待辦仍被計入總數，以及中途切換內容時舊 AI 回覆或動作計畫影響新畫面的問題。
- 自訂 Provider 新增「試送訊息」，可實際驗證模型回答；未儲存的連線變更會提示先儲存。HTTP 設定拒絕、授權、額度與服務錯誤各自顯示正確原因。
- Provider 設定寫入依序處理，避免同時保存時遺失連線；重試固定使用原 Provider，只有明確不支援結構化輸出才降級。
- PDF 匯入完成後釋放解析工作；卡片只在搜尋內容改變時重建索引。啟動失敗新增重新載入入口。
- Google 雲端在附件上傳後重新檢查裝置版本；遇到待處理衝突時保留現有快照。更新服務保留同檔案的驗證資料並防止備援版本倒退。

## 0.9.2 — 2026-09-05

- 「快速前往」改為依內容自然收尾，外框在「打賞」後立即結束；980px 設定頁實測外框寬 533.5px，右側額外未使用空間為 0px。
- 窄視窗仍會限制在可用寬度內，隱藏重複標題並保留橫向滑動；八個錨點、鍵盤操作、平滑定位與自動展開功能不變。
- Responses API 若模型明確拒絕選填的 `temperature`，澄境會自動移除後重試，並在本次執行期間記住該 Provider／模型的相容方式；支援溫度的模型仍使用原本的回答創意度。
- 新增正式 Electron 對話回歸：第一次模擬拒絕後成功重試，後續對話不再送出不支援參數；原始錯誤、API Key 與其他 Provider 設定不會被吞掉或洩漏。

## 0.9.1 — 2026-09-05

- 自訂 AI Provider 新增每組連線獨立的 Chat Completions／Responses API 模式；既有連線無須遷移，預設維持 Chat Completions。
- Responses 模式改送 `/responses`、`input`、`instructions` 與 `max_output_tokens`，同時支援頂層 `output_text` 與巢狀 output content 回覆；結構化輸出不相容時仍沿用既有安全降級。
- 遠端 Responses Gateway 固定使用 `store: false`；Ollama 依官方非狀態式方式送出完整必要歷史，不使用 `previous_response_id` 或 conversation。
- 設定頁頂端新增八個五語快速錨點，可平滑前往語言、AI、外部整合、更新、快速記錄、外觀、備份與打賞；鍵盤與減少動態偏好均保留。
- AI 設定改依目前引擎整理：OpenRouter 自動展開自己的模型區，Gemma 4 或自訂 Provider 會將它收合；自訂 Provider 只在選用時自動展開，未選設定仍可手動查看。
- OpenRouter 金鑰、Gemma 本機模型與自訂 Provider 只顯示目前引擎真正需要的控制；回答創意度與本機知識搜尋移到「所有 AI 共用」，功能、資料與原設定值完全保留。
- OpenRouter 三個精選模型中的 Gemini 已更新為 `google/gemini-3.8-flash`，其餘模型與排列維持不變。
- 目前引擎控制改用有實心層次的統一控制面，容量、Runtime、狀態、主要動作與隱私說明共用清楚骨架；未啟用的 MCP 則收成摘要列，錨點會先展開再定位。

## 0.9.0 — 2026-09-04

- 新增本機 MCP 伺服器，Codex、Claude Code 與其他相容工具可搜尋及讀取澄境，並依使用者選擇新增或修改筆記、待辦、白板、看板與神經元關係。
- MCP 預設關閉且啟用後預設唯讀；另提供「每次確認」與「允許寫入」。伺服器只綁定 `127.0.0.1`、驗證 Host／Origin、使用獨立加密權杖，並限制請求大小。
- MCP 不提供永久刪除。修改既有資料前必須比對最新 `updatedAt`，避免外部工具覆蓋同時發生的編輯；每次外部寫入形成單一可復原紀錄，最近結果留在本機稽核清單。
- 設定頁可一鍵複製 Codex 設定或 Claude Code 指令；權杖可隨時更換，舊連線會立即失效。MCP 伺服器、權杖與資料橋接均以正式 Client 完成端到端驗收。
- 新增「自訂 AI Provider」，進階使用者可建立與切換多組 OpenAI 相容 Gateway 或本機 Ollama 連線；Ollama 預設使用 `http://127.0.0.1:11434/v1`。
- 自訂 Provider 支援模型清單、連線測試、結構化輸出降級與現有 AI 問答／動作計畫；遠端位址強制 HTTPS，HTTP 只允許 loopback，API Key 獨立加密且不進入備份。
- 兩項進階功能均收進可展開的低干擾控制面，完整提供繁中、簡中、英文、日文與韓文文案；1040px、120% 字級、實際 Electron 與正式 MCP Client 寫入均通過。

## 0.8.2 — 2026-09-04

- 移除 Google 登入按鈕不必要的固定最小寬度，直接刪除文字右側多餘空白，不把空白重新分配到兩側。
- 按鈕依各語言內容自然決定寬度，維持可見 G 前方 12px、圖文之間 10px、文字後方 12px；繁中 120% 介面下實測寬度 166.05px，未使用寬度為 0px。
- 新增按鈕左右留白、結構間距、垂直置中與多餘寬度的像素驗收，五種語言皆可見且不溢出。

## 0.8.1 — 2026-09-04

- 修正 0.8.0 在備份設定中只留下透明點擊區、沒有顯示 Google 登入按鈕的問題；原因是內嵌 PNG 資料損壞，與 OAuth 發布或品牌審查狀態無關。
- 登入入口改成由澄境直接繪製的中性膠囊按鈕，搭配 Google 官方本機 SVG 彩色 G 與五語操作文字，不再依賴整張 PNG。
- 按鈕維持 40px 高度、清楚焦點外框與低調 hover 層次；即使圖示載入失敗，文字與操作區仍會顯示。
- 自動備份 QA 新增按鈕背景、文字、尺寸、SVG 實際載入與非 data URI 驗證，並在連結前保存視覺截圖，避免再次只測「可以點」卻漏掉不可見狀態。

## 0.8.0 — 2026-09-04

- 備份設定重整成「Google 雲端」與「本地」兩張卡片；兩者可獨立開關並同時執行，手動 JSON、Markdown 與檔案復原收進本地進階工具。
- Google 雲端只要求非敏感的 `drive.appdata`，以系統瀏覽器、PKCE、state 與桌面 loopback OAuth 登入，無法查看使用者其他 Drive 檔案。
- 雲端預設每 30 分鐘在應用開啟且閒置時備份；內容未變更會跳過，附件依 SHA-256 去重並以串流上傳，不把本機 AI 模型、API 金鑰或 OAuth token 放進備份。
- 固定保留「目前」與「前一天」兩個救援層；超過 48 小時的歷史快照與未引用附件會清理，但目前版本不會因久未開啟而刪除。
- 「緊急復原前一天」移到預設收合的獨立警告區，明示只供誤刪又同步時救援；覆蓋前另建本機安全副本並再次確認。
- 新裝置與多裝置加入雲端世代檢查；若雲端已有內容或被另一裝置更新，會暫停上傳並要求使用者明確復原或取代，不會靜默互蓋。
- macOS 的 Google refresh token 改用澄境本機 AES-256-GCM 靜默保存，避免未簽章 App 跳出鑰匙圈密碼視窗；Windows 繼續使用無提示的 DPAPI。
- Google Desktop Client Secret 不提交至程式碼庫；發行建置從開發機鑰匙圈或 CI 環境變數注入。加入真實 Google Drive 登入、上傳、復原、安全副本、測試資料清理與撤銷授權驗收。

## 0.7.8 — 2026-09-03

- 修正 Mac 視窗縮小或開啟右側 AI 面板後，頂端搜尋說明文字換行並超出搜尋框的問題。
- 搜尋框改依主工作區實際可用寬度漸進收合：先隱藏快捷鍵，再只保留固定 38×38 的放大鏡按鈕，不受側欄或右面板影響。
- 更窄時，頂端模型狀態與 AI 按鈕同步收成圖示，保留頁面標題、復原／重做與必要操作，整列始終維持原本 68px 高度。
- 搜尋文字即使尚未收合也固定單行並以省略號處理；圖示模式仍保留完整 tooltip、鍵盤快捷鍵與螢幕閱讀器名稱。
- 新增 1100px、120% 字級、右側 AI 開啟／關閉的實際容器寬度驗收，確認 458px 與 848px 兩種頂端列都沒有溢出。

## 0.7.7 — 2026-09-03

- 修正「整理這個白板的重點」被誤判為資料修改命令，導致 Gemma 4 與 OpenRouter 都顯示「沒有提出需要修改澄境的動作」。
- 純摘要、解釋與重點整理現在走一般 AI 回答；只有明確要求新增、移動、重排、更新或刪除內容時，才自動進入可預覽的 AI 動作流程。
- 使用者主動開啟 AI 動作後，如果模型提供有效摘要但判斷不需要修改資料，澄境會直接顯示摘要，不再把零項變更當成錯誤。
- 新增相同白板問題的一般問答、零變更摘要與本機 Gemma 真實生成驗收，兩種模型共用的意圖分流均已覆蓋。

## 0.7.6 — 2026-09-03

- 回補 [Transformers.js 官方 PR #1681](https://github.com/huggingface/transformers.js/pull/1681)，修正 Gemma 4 在 WebGPU 生成時遺失 `num_logits_to_keep=1` 的上游效能問題。
- 修正前會替提示詞的每個 token 計算完整 262,144 詞彙 logits；數百字摘要因此可能產生數百倍多餘運算，最後被 60 秒安全機制停止。
- 修正後只計算生成下一個 token 真正需要的最後一組 logits；沒有縮短卡片內容、摘要輸出或一般對話能力來掩蓋問題。
- 鎖定 Transformers.js 4.2.0 並在安裝、測試與建置時自動驗證回補；未來套件版本改變時會停止建置，避免把舊修正誤套到新版。
- 新增真實 ONNX feed 回歸測試與正式 bundle 驗證，確保 Windows x64、Windows ARM64 與 Mac 封裝都包含相同修正。

## 0.7.5 — 2026-09-02

- 正式新增 Windows ARM64 與 Intel／AMD x64 個別安裝程式；改用澄境自製的原生安裝流程，避免 Windows ARM 只建立捷徑卻遺失主程式。
- Windows 執行檔改用 `ChengJing.exe` 英文檔名，桌面捷徑與產品名稱仍維持「澄境」。
- Windows 改用原生標題列、Segoe UI、72px 收合側欄、Ctrl／Alt 快捷鍵與系統匣快速記錄；macOS 交通燈安全區與 AppKit 快速輸入維持原樣。
- Windows 系統匣支援預設 `Ctrl+\`、自訂全域快捷鍵、登入時背景啟動、關閉後留駐、重新顯示與完整結束。
- Windows 傳統選單列預設隱藏，按 Alt 才暫時顯示完整功能；「檔案」內仍保留「檢查更新…」。GitHub API、Cloudflare Worker 與 Atom Feed 會依目前處理器選擇、下載及驗證對應 EXE。
- 新增 Windows ICO／系統匣圖示、單一執行個體保護、五語 Windows 文案、雙架構 PE／ASAR 封裝驗證與 Windows 介面 QA。

## 0.7.4 — 2026-09-02

- 每筆待辦右鍵新增「新增子項目」，支援多層結構、縮排線與整棵子樹完成進度。
- 最後一個子項目完成時主項目自動完成；重新開啟子項目會重新開啟所有上層，直接完成主項目則完成整棵子樹。
- 子項目繼承來源卡片與初始期限；卡片／日誌 Checkbox 主項目的自動狀態會同步回原編輯器。
- 刪除主項目前會依子樹數量確認，並清理所有子項目與第二大腦連線；明確父子關係在第二大腦中以結構線呈現。
- 今日與日誌只顯示主項目；資料庫與搜尋仍保留每筆子項目。五語、1100px／120% 與既有待辦回歸通過。

## 0.7.3 — 2026-09-02

- 隻言片語、卡片庫卡片與看板卡片新增「新增為待辦 · 未排期」，原內容保持不變。
- 卡片／看板建立的待辦保留來源卡片，可從待辦返回；隻言片語建立獨立待辦，不強迫先轉成卡片。
- 看板詳情在 Checklist 下方提供直接動作；新增的是全域待辦，不混入卡片 Checklist。
- 同一來源的相同未完成待辦會防重；不同來源同名內容不會誤判，已完成工作仍可重新建立。
- 防重走輕量來源索引，不掃描整張待辦資料庫；五語、1100px／120% 與既有待辦／第二大腦回歸通過。

## 0.7.2 — 2026-09-02

- 修正 PDF 卡片上方只出現不可操作白色區塊的問題；移除失效的 Chromium PDF embed。
- PDF 頂端改為文件來源卡，顯示檔名、頁數、容量、儲存副本與移除附件；第一頁由本機 PDF.js 真正渲染。
- 點擊預覽可在澄境中央開啟完整閱讀器，支援換頁、頁碼跳轉、鍵盤、縮放與符合寬度，關閉後回到原卡片。
- 移除原始附件不會刪除已擷取文字；損壞或無法解析的 PDF 會顯示清楚降級狀態，不再留下白塊。
- PDF 執行核心維持按需載入；淺色、深色、墨色、五語、1100px 與 120% 字級專項驗收通過。

## 0.7.1 — 2026-09-02

- 卡片、看板卡片、待辦、隻言片語、劃記與白板內容的右鍵選單統一新增「編輯」，並放在第一項。
- 卡片維持完整編輯頁；待辦、片語與劃記使用同一套輕量編輯浮層；白板文字與區段留在原處編輯。
- 待辦若來自卡片 Checkbox，修改文字會同步寫回原卡片；從看板編輯卡片後仍返回原看板。
- 單純瀏覽日期產生的空白日誌改為未成形草稿，不進入卡片庫、資料庫、搜尋、第二大腦或 AI 本機內容範圍。
- 真正輸入內容或修改標題後，日誌才成為正式內容；曾寫過再清空的日誌不會被誤判為草稿。
- 舊空白日誌只在沒有標籤、附件、待辦、白板／看板位置、共享或神經元關係時於背景清理。
- 新增右鍵編輯與空白日誌完整 QA；第二大腦大資料量、待辦同步、白板、看板與知識架構回歸通過。

## 0.5.9 — 2026-08-28

- 修正不同 Cloudflare 資料中心可能保留舊更新索引，導致 0.5.7 顯示「GitHub 最新 0.5.3」且無法更新的問題。
- Worker 以 KV 最新版作為快取世代並比較 KV／節點快取版本；舊世代不再命中，較新的 KV 會自動修復目前節點。
- App 會忽略比自身更舊的更新來源並繼續下一層；檢查失敗時清除畫面上的舊版資訊，避免錯誤數字殘留。
- 以原始 0.5.7 App 實測強制備援，成功取得 0.5.8、正確 DMG 與摘要雜湊。

## 0.5.8 — 2026-08-28

- 本機 Gemma 的第二大腦改為安全模式：18 個神經元、24 組候選、最多 8 條連線與 768-token 輸出，避免 ONNX Runtime `SafeInt` 容量溢位。
- 本機模型底層輸出硬上限改為 1,024 token，推論單列排隊，並加入 60 秒可中止條件；過長輸入、容量溢位與逾時改顯示五語友善錯誤。
- 本機關聯 JSON 不完整時不再自動重跑，整理完成後也不會緊接著生成今日反思；反思可單獨以 18 個神經元與 384-token 安全範圍生成。
- OpenRouter 的完整全腦分析範圍、JSON 修復與自動更新反思維持不變。
- 新增本機推論預算、ORT 溢位辨識與第二大腦安全脈絡單元測試；第二大腦、五語與本機 ONNX 專項回歸均通過。

## 0.5.7 — 2026-08-28

- 快速隻言片語改用原生 AppKit `NSPanel` 與 `NSTextView`，修正雙拼雖能輸入文字、macOS 候選詞框卻不出現而無法選字的問題。
- 原生 Carbon 全域熱鍵與原生輸入框合併在同一個 `LSUIElement` 背景小工具；視窗預先建立，保留其他 App／全螢幕狀態下的毫秒級瞬發。
- 實機以繁體中文雙拼輸入 `ni`，展開候選後選第 2 個候選字得到「妳」；組字期間的 Enter 交給輸入法，組字完成後 Enter 才保存。
- 原生文字透過本機管線寫入澄境 IndexedDB，成功回讀後才顯示已儲存並收起；失敗時保留原文可重試。
- 新增 `native-appkit`、`native-nstextview` 與 IME ready 封裝診斷；正式背景 App QA 回報全部通過，顯示命令主程序耗時約 0.13ms。

## 0.5.0 — 2026-08-28

- 全應用新增上一步／下一步按鈕與 ⌘Z／⌘X 資料動作歷史；白板沿用自己的細緻歷史，文字輸入保留原生編輯行為。
- 白板、看板列表、看板卡片與隻言片語新增 ⌘C／⌘V 與右鍵剪貼；Electron 44 使用原生非同步 ClipboardItem API，同時保存人類可讀文字與澄境物件資料。
- 看板 Enter 直接建立卡片、Option+Enter 明確插入換行；Checklist 可在看板詳情直接同步勾選，篩選間距、欄位白邊與選中 hover 位移一併修正。
- 收件匣導覽移除，快速記錄預設保存為隻言片語；資料庫升級會把舊 inbox 卡片轉為 active，完整保留在卡片庫。
- 隻言片語右鍵新增送白板、加入看板與貼上新片語；原片語不會因轉換而消失。
- 新增 macOS Tray、570×188 快速輸入窗與預設 ⌘\ 全域快捷鍵；設定可錄製新組合鍵，並可選擇登入時只在背景啟動。
- 主視窗與快速窗共用同一份本機 IndexedDB；背景啟動不建立主視窗，需要時才從 Tray 顯示。
- 視窗模式 Logo 右移避開交通燈，全螢幕模式恢復靠左；Brand、Top Bar 與右側面板頂端統一高度。
- 新增快速快捷鍵持久化 Node 測試、全域操作 QA 與真實 Electron 背景 Tray QA；既有完整回歸重新通過。

## 0.3.17 — 2026-08-27

- 左側底部新增「許願池」，在澄境內以右側面板開啟，不跳出瀏覽器；淺色、深色與墨色共用同一套無邊框、低彩度設計。
- 許願池支援匿名願望與第二層回覆。每台 App 會從 100 種跨物種中文名稱中取得匿名身分，使用者可隨時重新生成名稱與匿名 ID。
- 新增管理員登入；密碼只存在 Cloudflare Secret，不寫進 App 或 GitHub。管理員回覆固定顯示為「管理員」與醒目徽記，並可刪除原始願望或任一回覆。
- 後端使用按 UTC 月份分片的 SQLite Durable Objects，不建立單一全域瓶頸；每頁最多 20 則願望、每則先帶 3 則回覆，其餘以游標按需載入。
- 願望與回覆使用複合索引；公開寫入每 15 分鐘最多 8 次、管理員登入最多 5 次。Cloudflare 只保存加鹽後的來源雜湊，不保存原始 IP。
- 修正 Gemma 4 啟動時從 jsDelivr 動態載入 ONNX Runtime，導致 `no available backend found` 與 `Failed to fetch dynamically imported module` 的問題。
- ONNX Runtime 的 asyncify `.mjs` 與 22 MB WASM 現在隨 App 打包，Transformers.js 明確改讀本機路徑；Gemma 權重仍由 Hugging Face 下載並保存於本機快取。
- 本機執行檔以 130-byte ONNX 測試模型完成 `[1, 4, 9, 16, 25, 36]` 推論；封裝 App 再使用既有 2.9 GB Gemma 權重真正生成繁體中文，過程沒有 CDN、backend 或頁面錯誤。
- 28 組 App QA、66 個前端單元測試、16 個 Electron Node 測試、3 個 Worker 整合測試、正式 App 與 DMG 內 App smoke 全數通過。

## 0.3.16 — 2026-08-27

- 新增完整「上班族一日」驗收旅程：以繁體中文真實建立會議卡片、標籤、屬性、劃記、期限待辦、日誌、隻言片語、網址與 Markdown，再驗證置頂、右鍵、資料庫、第二大腦、備份及跨分類 AI 動作。
- 修正卡片「新增屬性」按鈕沒有反應；現在可直接輸入屬性名稱與內容，並支援修改內容、移除屬性、重複名稱提醒及五語介面。
- 修正從白板開啟中央卡片後，白板資訊列與工具列仍浮在卡片上方，可能攔截「返回卡片庫」的問題；卡片工作面現在完整位於白板控制層上方。
- OpenRouter 路由模式的「預設」標籤由 11px 提升為 12px，深色與墨色介面不再出現低於全域閱讀基準的小字。
- 白板關係線右鍵驗收改用固定測試連線並先顯示全部內容，不再受前一步心智圖平移視窗影響而偶發找不到線。
- 26 組端到端 QA、61 個前端單元測試、16 個 Electron Node 測試、正式 App 與 DMG 內 App smoke 全數通過。
- 以封裝後 v0.3.16 和既有 Debug 資料真實操作：OpenRouter 成功產生 11 個卡片轉白板變更，套用後得到 4 張非空卡片、3 個區段與 3 條關係線，沒有空白卡片或 `invalid-edge-reference`。

## 0.3.15 — 2026-08-27

- 修正卡片轉換白板時，AI 預覽已有歸納內容，實際建立的區段卻是「新的區段」、卡片是「新的卡片」且內文空白的問題。
- AI 動作解析新增 `name／heading／body／details／summary／note／markdown／bullets`，並支援 `parameters／arguments／args／payload／data` 巢狀欄位。
- 轉換計畫若缺少白板或區段 title、卡片 title／content、文字節點 text，會用相同模型與路由模式自動修復一次完整資料欄位。
- 修復請求仍失敗或模型再次漏欄位時，會以已歸納的 description 實體化標題與內文，禁止寫入空白區段與卡片。
- 提示詞明確區分：description 只供套用前預覽，title／content／text 才是真正寫入白板的資料。
- 專項驗收以第一次刻意漏欄位、第二次補齊的真實流程測試；最終區段標題、三張卡片標題與 plainText 均非空，原卡片不變。

## 0.3.14 — 2026-08-27

- OpenRouter 新增「平衡／極速／省錢」三種 Provider 路由模式，預設改為平衡。
- 平衡模式使用價格排序並偏好最近中位吞吐量至少約 45 tokens/s 的 Provider；門檻為軟性偏好，較慢節點仍可在必要時備援。
- 極速模式使用 throughput 排序，與 OpenRouter Nitro 路由等價；速度優先，費用可能較高。
- 省錢模式明確使用 price 排序，適合背景整理與不急著等待的工作。
- 一般 AI 對話、結構化 AI 動作、卡片轉白板、第二大腦整理、今日反思與 JSON 重試都使用同一個路由設定。
- Electron 主程序只接受 balanced／speed／economy 白名單，renderer 無法任意注入 OpenRouter provider 欄位；舊版或未知值安全回到平衡。
- 路由設定會自動保留；升級前沒有這個欄位的使用者會取得平衡預設。
- 新設定採三欄低彩度平面選擇，不增加外層卡片或高彩度速度提示；五語、1040px 與 120% 字級均無溢出。

## 0.3.13 — 2026-08-27

- 日誌「選擇日期」由紙張標題區移到上方日期導覽列，緊鄰「今天」，相同性質的操作形成同一組。
- 「今天」與「選擇日期」維持相同 36px 高度、0px 垂直位移、6px 間距與一致圓角，不增加外層色塊或多餘動畫。
- 日誌紙張內只保留「回顧這一天」，日期導覽與內容操作的層級更清楚。
- 七日列樣式改為明確的 `journal-week-days` scope，避免日期選擇器文字意外繼承星期按鈕樣式。
- 五語、120% 字級、1100px 響應式與月曆展開位置重新驗收，無水平溢出。

## 0.3.12 — 2026-08-27

- 修正卡片轉換白板時，AI 關係線引用節點標題或常見 `source`／`target` 欄位，套用卻回報 `invalid-edge-reference` 並回滾整批變更的問題。
- 關係線現在接受 `sourceRef`／`targetRef`、`source`／`target`、`from`／`to`、節點物件與常見 id 欄位。
- 套用時會以 tempId、節點標題、動作說明、既有卡片標題與唯一序號解析起點與終點；標點、空白與全半形差異不再造成失配。
- 單條連線仍無法辨識時，只略過該連線並明確顯示完成／略過數量；其他白板、區段與卡片安全建立，不再整批回滾或顯示紅色錯誤。
- 缺少 description 的關係線在預覽中改顯示「建立關係線」或關係名稱，不再直接暴露 `create_board_edge`。
- 卡片右鍵選單新增「轉換成白板」，會自動選定該卡片並開啟同一套 AI 預覽；不必先進入卡片閱讀頁。

## 0.3.11 — 2026-08-27

- 日誌標題旁新增「選擇日期」，沿用待辦的自製月曆，可跨月份直接開啟任意日期；不混入待辦專用的明天、一週後與清除期限操作。
- 日誌日期月曆支援鍵盤方向鍵、Page Up／Page Down、Esc、今天標記與目前日期，五種介面語言同步完成。
- 卡片底部新增「轉換成白板」：使用目前設定的 OpenRouter 或 Gemma 4，自動送出轉換要求並先顯示可取消的變更預覽。
- 卡片轉白板會保留原卡片，建立全新白板，再將核心主張、證據、問題、決策與下一步拆成精簡卡片、區段與必要連線。
- AI 動作新增建立、修改與刪除白板，以及跨白板 `boardRef`；卡片、白板、日誌、待辦與隻言片語可從任何目前內容互相建立或更新。
- 跨分類動作仍遵守「搜尋其他本機卡片」開關；關閉時只傳送目前內容與必要目的地，不讀取其他卡片、待辦或隻言片語內容。
- 卡片的「收藏」語意統一改為「置頂」；既有 favorite 資料直接沿用，不需遷移也不會遺失。
- 卡片庫與資料庫新增「已置頂」集合，置頂卡片在一般列表內也優先顯示；卡片、白板節點與右鍵選單使用一致的 Pin 圖示與文字。
- 修正開啟中的卡片移到垃圾桶後閱讀層仍擋住卡片庫的問題。
- 啟動資料同步改為單一 Promise，避免 React Strict Mode 重跑時替同一個編輯器待辦建立兩份神經元。

## 0.3.10 — 2026-08-27

- 收件匣卡片的文件圖示不再擁有獨立方框、底色、圓角或 hover 動畫，只保留符號本身。
- 保留原有 34×34 對齊空間，避免圖示、標題與摘要因視覺簡化而改變位置。
- 收件匣卡片成為唯一會淡變的表面；圖示自然隨卡片背景顯示，不再發生亮起與暗掉速度不同步。
- 新增圖示背景透明、圓角為 0、transition 為 none 的網頁與封裝 App 自動驗收。
- 再次逐項比對卡片、圖示、標題、摘要與底部資訊，hover 前後的 x／y／寬／高完全相同。

## 0.3.9 — 2026-08-27

- 修正收件匣卡片 hover 時 padding 從 5px 變成 9px，造成背景尺寸改變與文字重新換行的問題。
- 收件匣卡片父層改為透明，不再同時顯示外層大背景與內層 hover 背景。
- 每張收件匣卡片從一般狀態起就使用固定 14px 左右 padding、10px 圓角與獨立實心背景。
- hover 只以 120ms 將背景從 `surface-1` 淡變為 `surface-2`；不再改 padding、寬高、transform 或文字可用寬度。
- 修正僅限定於收件匣的待整理／已整理卡片，不改動今日、搜尋結果或其他列表。
- 新增卡片本體、圖示、標題、摘要與底部資訊的 hover 前後幾何逐項比對，以及封裝 App 專項驗收。

## 0.3.8 — 2026-08-27

- 最新版 DMG 開啟後，更新視窗按鈕改為「關閉澄境並開始取代」，不再只關閉更新框。
- 按下按鈕會透過 Electron main process 真正結束澄境，讓 Finder 可以立即將 DMG 中的新版拖進「應用程式」並選擇取代。
- 更新完成說明同步改為先關閉澄境、再拖入新版，繁中、簡中、英文、日文與韓文均更新。
- macOS 26 Tahoe 的「檢查更新…」原生選單加入單色循環更新 SF Symbol，與系統其他選單項目的圖示風格一致。
- macOS 27 Golden Gate 不傳入自訂 icon，維持全選單無圖示的系統外觀。
- 使用 Electron `nativeImage.createMenuSymbol()` 建立原生 template 圖示，不使用彩色圖片或自繪尺寸。
- 新增 Tahoe／Golden Gate 版本條件單元測試、封裝 App 選單 icon 快照、quit bridge 與更新完成按鈕端到端驗收。

## 0.3.7 — 2026-08-27

- macOS「澄境」應用程式選單在「關於澄境」下方新增「檢查更新…」。
- 原生選單入口送出與「設定 → 軟體更新 → 立即檢查」相同的手動檢查事件，並自動前往軟體更新區顯示結果。
- 啟動自動檢查由「每次啟動」改為「每天第一次啟動」；使用本機日曆日判斷，不以固定 24 小時計算。
- 成功取得 GitHub／Cloudflare 更新資料後才記錄當天；網路失敗不會消耗當日機會，下次啟動仍會再嘗試。
- 同一天重新開啟或重新載入不重複檢查；隔天第一次啟動重新檢查。
- 繁中、簡中、英文、日文與韓文原生選單及設定說明同步更新。
- 新增純選單模板單元測試、每日日曆日測試、選單事件端到端測試與封裝 App 原生選單快照驗收。

## 0.3.6 — 2026-08-27

- 修正中央卡片工作面位於 macOS 視窗拖曳區上方，導致「返回卡片庫」看得到卻無法點擊的問題。
- 卡片工作面與返回按鈕明確設定為 `no-drag` 互動區，真實滑鼠點擊不再被 Electron 視窗拖曳行為攔截。
- 卡片返回列整體下移 42px，直接避開 macOS `hiddenInset` 原生標題列命中區。
- 卡片底部「AI 動作」改為單純開啟 AI，不再自動把摘要句塞進輸入框。
- AI 輸入框上方新增低調的「推薦 Prompt／摘要這張卡片」按鈕，只有使用者點擊後才帶入可編輯的推薦文字。
- 一般開啟 AI 時會清除殘留的快捷 Prompt；白板等明確快捷動作仍可保留其專屬草稿。
- 「同步搜尋本機卡片」改為可操作的範圍開關；關閉時只使用目前內容，開啟時才額外搜尋其他本機卡片。
- 開關加入 `aria-pressed`、開啟／關閉說明與五語提示，狀態不再只是無法點擊的裝飾標籤。
- 收件匣「待整理／已整理」移除雙層色塊、陰影與位移，改為固定尺寸、透明底與 2px 底線的平面分頁。
- 新增 `app-region`、空白輸入框、推薦按鈕顯示與按需帶入文字的網頁及封裝 Electron 專項驗收。

## 0.3.5 — 2026-08-27

- 完整移除速度過慢的「知識問答」分類、畫面、RAG 索引、OpenRouter embeddings 與本機 E5 embedding 管線。
- 資料庫升級時自動刪除舊語意索引表；啟動後清理澄境專用的 Multilingual E5 模型快取。
- AI 側欄重新開啟時會恢復目前卡片、白板或整體空間最近使用的對話，不再關閉後消失。
- AI 標題列新增「開啟新對話」；舊對話仍保存在本機資料庫與完整備份中。
- 修正第一次以 Enter 送出後文字留在輸入框的問題；中文輸入法組字期間的 Enter 只確認文字，不會提前送出。
- 卡片改為中央主工作面，完整覆蓋卡片庫清單；AI 動作會在右側開啟，不再取代正在閱讀的卡片。
- 卡片左上新增「返回卡片庫」，AI 上方新增「參考卡片＋卡片名稱」，明確標示目前上下文。
- 修正卡片庫知識樹缺少 React key 的既有警告，廣域 QA 不再因此誤報失敗。

## 0.3.4 — 2026-08-27

- 編輯器劃線改為依外觀切換的低彩度赭金色，不再固定使用刺眼亮黃色。
- 深色模式使用深土黃底與暖白字；墨色模式再降低亮度，維持內斂的紙墨質感。
- 淺色模式使用柔和麥金底與深墨字，三種外觀都保留清楚的重點辨識。
- 移除新劃線內容中的固定色碼，之後切換外觀會立即套用正確配色。
- 舊版已保存的亮黃色劃線也會由新主題樣式接手，不需要重新劃記。
- 新增三主題劃線、舊資料相容與文字對比專項驗收。

## 0.3.3 — 2026-08-26

- AI 助理完成訊息改用現有 marked＋DOMPurify 安全 Markdown 管線。
- 新增適合窄側欄的標題、段落、清單、引用、程式碼、分隔線、表格與連結樣式。
- 使用者訊息與生成中的串流保持純文字，最終回答才轉成 Markdown，避免逐 token 排版負擔。
- 存成 AI 卡片時保存清理後 HTML，同時保留原始 Markdown 純文字。
- 外部連結補上新分頁與 `noopener noreferrer`；腳本、追蹤圖片與危險網址仍被移除。
- 使用者訊息泡泡改為標準靠右純文字泡泡，移除框內「你」標籤；身分只保留於 aria-label，舊訊息顯示時同步 trim 前後空白。
- 新增 Markdown 元件單元測試、完整回答端到端測試與封裝 Electron AI 面板專項驗收。

## 0.3.2 — 2026-08-26

- 白板底部工具列新增上一步／下一步按鈕，支援 `⌘Z`、`⌘X` 與 `⇧⌘Z`；文字欄中的 `⌘X` 仍為剪下。
- 新增最多 80 層的白板資料歷史，涵蓋節點、連線與白板內卡片文字；新分支會清除舊 redo。
- AI 整批建立的白板區段、卡片與連線可由同一套歷史移除與恢復。
- AI 動作規劃新增原子卡片、短標題、短段落／逐行 Bullet 與重要資訊保留規則。
- 過長的 AI 自由文字自動轉為實心卡片，避免透明文字與連線互相穿透。
- Inline Bullet 轉為分行純文字與真正 HTML 清單；白板卡片預覽改為段落／清單區塊。
- 關係線改用智慧標籤，依來源與目標節點邊界選擇上、下或側邊空白位置。
- 新增操作歷史分支、AI 匯入復原、文字剪下保護、Bullet 行距、標籤可見與不碰撞專項驗收。

## 0.3.1 — 2026-08-26

- 左下今日反思與展開閱讀均支援滑鼠滾輪／觸控板上下捲動，並隱藏 Firefox 與 WebKit 視覺捲軸。
- 預覽淡化提示會在捲到最底後消失，避免遮住最後一段內容。
- 展開紙頁提高到最高 960px 寬、760px 正文欄、16px 正文字級與更完整留白。
- Three.js HTML 神經元標籤限制為較低 z-index；展開反思以不透明實心表面完整遮住標籤。
- 五語今日反思提示改為 3～4 個短段落，不再要求單一長段落。
- 新增依語言句子邊界分段的顯示／保存保護，既有單段反思也會自動改善。
- 新增預覽滾動、展開滾動、隱藏捲軸、紙頁尺寸、標籤遮擋與自動分段專項驗收。

## 0.3.0 — 2026-08-26

- 卡片庫新增領域 → 主題 → 卡片的知識層級、中文輸入、右鍵管理、拖曳分類與內容不重複的資料模型。
- 統一新增筆記、網頁與檔案入口，讓 PDF、DOCX、Markdown、圖片、音訊與影片的可建立類型和篩選器一致。
- PDF 匯入會保存附件、抽取文字、建立 PDF 卡片並在應用內預覽。
- 新增 RAG 知識問答：內容分段、雜湊增量索引、語意召回、來源引用與點回原卡片。
- OpenRouter 模式使用 embeddings API；本機模式以 Multilingual E5 Small 建立 384 維向量，再由 Gemma 4 生成答案。
- 新增 AI 安全動作規劃器與 17 種白名單操作。動作先預覽，使用者確認後才實際寫入；刪除與覆寫有明確警告。
- 「請 AI 整理這張白板」會真正建立區段、卡片、連線、待辦、日誌與片語，不再只留下聊天回答。
- AI 修改卡片時保留既有富文字；修改或刪除編輯器待辦會同步回寫原文。
- 待辦成為第二大腦神經元，支援獨立待辦、編輯器待辦、手動連線、AI 語意連線、今日反思與刪除連線同步清理。
- 資料庫新增所有內容／卡片／待辦分流；待辦可搜尋、依來源標籤篩選、查看期限、完成／恢復與右鍵管理。
- 分享大腦新增待辦隱私開關，預設不分享工作與生活安排。
- 第二大腦工具列、資訊面板與今日反思改為低彩度實心表面，移除紫褐色、透明模糊與裝飾性彩框。
- 第二大腦 AI 連線加入 JSON schema、常見缺號修復、截斷內容回收與一次自動重試；最終錯誤改為可理解提示。
- 今日反思新增縮到最小與一鍵還原，不必在閱讀區、展開紙頁與完整隱藏之間二選一。
- 新增知識層級、PDF、RAG、本機 E5、AI 新增／修改／刪除、待辦神經元與資料庫專項端到端驗收。

## 0.2.10 — 2026-08-26

- 待辦頁依今天、過期、未來、無期限與完成分段；未來日期由近到遠。
- 以自製無邊框月曆取代原生日期欄位，加入今天、明天與一週後快捷選擇。
- 更新檢查加入 Cloudflare Worker、24 小時舊快取、30 天 KV 與 GitHub Release Feed 多層備援。
- GitHub Release 標題統一為「澄境筆記＋版本號」。

## 0.2.9 — 2026-08-26

- 新增低負載定時自動備份、空閒執行、原子寫入、保留份數與雲端同步資料夾提示。

## 0.2.8 — 2026-08-26

- 移除今日反思文末的生成時間與模型名稱。
- 今日反思預覽由固定高度捲動卡改為較寬、行距更舒適的無邊框閱讀面。
- 長文預覽不顯示內嵌捲軸，改用淡漸層提示尚有內容。
- 新增置中「展開閱讀」模式與低調背景遮罩。
- 展開紙頁依內容自然長高，只有超過視窗上限時才使用細緻捲軸。
- 新增收合閱讀、背景點擊、Esc 關閉、焦點轉移與 reduced-motion 相容。
- 五種語言新增展開／收合閱讀文案。
- 封裝後驗收新增模型資訊移除、閱讀模式寬度、焦點、遮罩與捲動行為。

## 0.2.7 — 2026-08-26

- 第二大腦「今日反思」改用 marked 解析 Markdown，修正粗體等語法直接顯示原始符號。
- 新增適合反思面板的標題、段落、清單、引用、程式碼、連結與表格排版。
- 所有轉換後 HTML 先經 DOMPurify 白名單清理，移除腳本、事件屬性、危險網址與遠端圖片。
- 資料庫與備份繼續保存原始 Markdown，不把顯示用 HTML 寫回資料。
- 今日反思提示改為溫暖、口語、連續的朋友式 insight，禁止標題、列點、表格與報告式術語。
- 反思自然使用「你」對話、具體關心與溫柔提問，同時保留不確定性、替代解釋與心理診斷安全邊界。
- AI 虛線關聯新增透明 10px 連續命中線，修正虛線空隙無法按右鍵的問題。
- AI 關聯右鍵選單與刪除流程新增端到端驗收；手動連線同步改善命中範圍。
- 新增安全 Markdown 單元測試與封裝後 Markdown 實際渲染測試。

## 0.2.6 — 2026-08-26

- 新安裝依 macOS 第一偏好語言自動選擇繁中、簡中、日文、韓文；其他語言預設英文。
- 原生 macOS 選單與應用介面使用同一套偏好語言判斷。
- 外觀的新安裝預設確認為「跟隨系統」，既有手動選擇仍會保留。
- 所有卡片型編輯器的核取方塊待辦自動同步至左側「待辦」。
- 編輯器待辦加入穩定來源 ID 與舊內容自動遷移，不以標題猜測身分。
- 編輯器與待辦列表之間的完成／恢復狀態雙向同步。
- 待辦新增表單加入可選填目標日期。
- 待辦右鍵選單新增設定、修改、移除期限；編輯器中的待辦也可直接右鍵操作。
- 刪除編輯器來源待辦時同步移除原文核取項目。
- 設定頁底部新增永久免費、無廣告的實驗性說明，以及綠界／PayPal 自願打賞入口。
- 打賞與期限介面完整支援繁體中文、簡體中文、英文、日文與韓文。
- 新增五語首次啟動、系統主題、打賞連結、跨頁待辦、雙向完成、ETA 與刪除同步專項驗收。

## 0.2.5 — 2026-08-26

- 第二大腦 AI 從關鍵詞／字面整理升級為語意關聯探索。
- 神經元分析加入最初記錄時間、最後更新時間、內容類型、正文與表面詞彙。
- 候選生成同時保留字面相近與沒有共同詞的跨主題配對；候選只是提示，不會直接建立連線。
- 新增語意關係類型：語意相關、共同脈絡、可能影響、目標與阻礙、前後延續、對照張力與互相強化。
- AI 連線必須提供兩端直接線索、慎重的假設理由與至少 0.62 的信心值；時間接近不能單獨證明因果。
- 神經元資訊面板新增另一端內容、關係類型、線索強度、時間差、理由與證據。
- AI 整理完成後會自動重寫今日反思，引用內容名稱與雙端線索，區分觀察、假設與替代解釋。
- 沒有產生合格新連線時保留既有 AI 連線；手動連線始終不受 AI 整理影響。
- 五種介面語言都具有同一套語意推理與安全邊界。
- 新增無共同詞語意候選、低信心拒絕、證據顯示與自動反思專項驗收。

## 0.2.4 — 2026-08-26

- 修正日誌的重點標示只改變文字外觀、卻沒有寫入左側「劃記」的問題。
- 日誌劃記新增重複內容保護、成功提示與重新載入持久化。
- 日誌與卡片編輯器的 12 個格式工具全部加入自訂滑鼠移入／鍵盤焦點說明。
- 更新視窗自動移除 GitHub Release 內的 SHA-256 校驗區塊與 Markdown 標題符號。
- 日誌與卡片劃記改為分開實際操作驗收，避免只測卡片卻誤以為日誌也正常。

## 0.2.3 — 2026-08-26

- 日誌正文區改用文字輸入游標，滑鼠移入即可辨識能直接打字。
- 白板與隻言片語正式加入標籤資料，日誌沿用卡片標籤並提供第一層操作入口。
- 卡片、白板、日誌、隻言片語共用同一個無邊框標籤選擇器。
- 每個標籤選擇器底部新增「新增標籤」，支援中文輸入法、Enter 與點擊外部保存。
- 資料庫標籤新增右鍵選單，可重新命名或全域移除。
- 重新命名會即時同步所有卡片、白板、日誌、隻言片語與第二大腦關鍵詞。
- 移除標籤會清除所有內容引用；同名重新命名會安全合併，不建立重複標籤。
- 新增完整標籤生命週期 QA，涵蓋中文 IME、英文、Enter、失焦、解除套用、右鍵、重新命名、全域刪除、重啟持久化與淺色視覺。

## 0.2.2 — 2026-08-26

- 真實 GitHub Release 下載驗收發現 Electron DownloadItem 完成事件與檔案落盤時序不一致。
- 更新下載器改由 Electron 主程序使用 Chromium 網路層串流寫入 `.part` 暫存檔。
- 每一段資料依序寫入並即時回報進度，完成後先同步至磁碟，再原子更名為 DMG。
- 檔案落盤後才進行大小與 GitHub SHA-256 digest 驗證，驗證通過才開啟。
- 下載中斷、逾時或驗證失敗會移除不完整檔，不會留下看似完整的 DMG。

## 0.2.1 — 2026-08-26

- 部署公開 GitHub 專案與正式 GitHub Releases 下載頁。
- 每次啟動後自動檢查 GitHub 最新正式 Release。
- 有新版時顯示目前版本、最新版本與 Release 更新內容。
- 新增「下載並手動更新」流程、即時下載進度與錯誤回饋。
- DMG 下載完成後核對檔案大小與 GitHub SHA-256 digest，再交由 macOS 開啟。
- 設定頁新增軟體更新區，可立即檢查版本或下載新版。
- 更新流程完整支援繁體中文、簡體中文、英文、日文與韓文。

## 0.2.0 — 2026-08-26

- 新增繁體中文、簡體中文、英文、日文與韓文完整介面。
- 導覽、日期、白板、卡片、右鍵、AI、匯入備份、第二大腦、分享 HTML 與 macOS 原生選單同步切換。
- AI 回答語言跟隨介面語言，既有筆記維持原文。
- 語言設定永久保存；新增五語逐頁、1040px／120% 字級與重啟持久化驗收。

## 0.1.7 — 2026-08-26

- 淺色、深色與墨色的主要卡片、面板、設定區與導覽全面改為無邊框。
- 重建三主題的實心明度階梯，以背景色差與留白取代外框。
- 墨色提高文字與相鄰面的清晰度，修正難以對焦的灰霧感。
- 墨色持續閱讀面移除半透明混色、backdrop blur 與不必要陰影。
- 保留輸入框實心底色、鍵盤 focus ring 與必要的內部分隔線。
- 新增三主題外框、色階、陰影、模糊與文字對比專項驗收。

## 0.1.6 — 2026-08-26

- 新增白板重新命名按鈕，新增白板後直接進入命名狀態。
- 修正雙拼／拼音輸入法組字期間過早寫回卡片標題，造成只能輸入英文拼音。
- 找出並修正 OpenRouter 聊天請求標頭含中文導致 TypeError 的真正斷線原因。
- OpenRouter 改用 Electron Chromium 網路層，支援 macOS 系統代理與憑證。
- 新增官方 `/api/v1/key` 金鑰驗證與清楚的 HTTP／網路錯誤分類。
- OpenRouter 金鑰改為澄境本機 AES-256-GCM 加密檔，不再使用 macOS 鑰匙圈。
- 新增加密持久化、明碼排除、正式 IPC、聊天端點與中文 composition 專項驗收。

## 0.1.5 — 2026-08-26

- 日誌日期列改為星期加月／日，並修正台北時區的「今天」判斷。
- 修正待辦核取方塊與第一行文字錯位。
- 第二大腦的日誌神經元改用內容標題或首句命名。
- 編輯器螢光筆與左側劃記資料庫正式同步，並避免重複劃記。
- 編輯器工具列改為中性實心墨面。
- 白板選取模式改用正常箭頭，連線模式才使用十字游標。
- 移除 AI 導師入口、檢視、樣式與程式碼；保留舊資料相容性。
- 新增日誌語意、日期、勾選對齊、游標與劃記持久化專項驗收。

## 0.1.4 — 2026-08-25

- 修正資料庫左側標籤按鈕沒有作用，新增標籤篩選狀態與正確筆數。
- 新增表格／看板共用的批次選取、目前結果全選、垃圾桶與永久刪除。
- 第二大腦預設顯示全部神經元名稱，並保留重點標題模式。
- 新增具有隱私選項的「分享大腦」，可匯出不依賴網路的單一互動 3D HTML。
- 修正白板連接點 hover 時位置漂移，放大前後圓心保持不動。
- 新增資料庫、分享檔、隱私排除、離線 3D 與連接點幾何驗收。

## 0.1.3 — 2026-08-25

- 修正卡片編輯器三點選單，新增副本、封存、垃圾桶、還原與永久刪除。
- 卡片、白板、待辦、劃記、日誌與隻言片語新增情境右鍵操作。
- 卡片庫全面改為單色、低輪廓的實心卡片。
- 新增日式內斂「墨色」主題並重整全應用材質。
- 新增隻言片語、釘選、快速保存、轉卡片與刪除。
- 新增 WebGL 3D 第二大腦、神經元權重、樹突、滑鼠／WASD 操作、手動連線與刪線。
- 新增 OpenRouter／Gemma AI 自動關聯與每日反思；AI 推論可撤回且不作心理診斷。
- JSON／Markdown 備份納入片語、第二大腦連線與每日反思。
- 新增第二大腦整合驗收與墨色可讀性驗收。

## 0.1.2 — 2026-08-25

- 全面重做白板連線、心智圖與右鍵互動。
- 白板卡片改為單色無邊框設計。
- 工具列移到底部中央並增加繁中 tooltip。
- 白板選擇器改成點擊展開，右上搜尋正式可用。
- 新增白板專項自動驗收。

## 0.1.1 — 2026-08-25

- OpenRouter／Gemma 頂部狀態改為可點擊設定入口。
- 日誌新增「今天」快速返回。
- 側欄收合控制移到底部。
- 全面重整深色文字色階、中文字級、字距與行高。
- 新增 90%／100%／110%／120% 文字比例。
- 卡片庫新增第一層「新增卡片」與網址收藏欄。
- 網址卡片點擊後開啟原始網頁。
- 修正深色 Logo 與標籤粗框。
- 新增 WCAG 對比、繁中排版、1100px／120% 響應式驗收。

## 0.1.0 — 2026-08-25

- 第一個可安裝版本。
